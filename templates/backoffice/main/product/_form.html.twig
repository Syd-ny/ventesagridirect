<div class="container mx-4 mt-2 mb-2">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-outline-primary mb-3 mt-3 mx-3">{{ button_label|default('Sauvegarder') }}</button>
        <a href="{{ path('product_list') }}" class="btn btn-sm btn-outline-info mx-3 mt-3 float-end">Retour à la liste</a>
    {{ form_end(form) }}
</div>