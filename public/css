body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background-color: #f0f0f0;
}

header {
  background-color: #2c3e50;
  color: #fff;
  text-align: center;
  padding: 20px 0;
}

header h1 {
  font-size: 36px;
  margin: 0;
}

header p {
  font-size: 18px;
  margin-top: 10px;
}

main {
  max-width: 1200px;
  margin: 20px auto;
  padding: 20px;
  background-color: #fff;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.products {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}

.product {
  flex: 1;
  background-color: #fff;
  border: 1px solid #ddd;
  padding: 20px;
  text-align: center;
}

.product img {
  max-width: 100%;
  height: auto;
  border: 1px solid #ddd;
  padding: 10px;
  background-color: #fff;
}

.product h3 {
  font-size: 24px;
  margin-top: 10px;
}

.product p {
  font-size: 16px;
  margin-top: 10px;
}

.about-us {
  margin-top: 20px;
  text-align: center;
}

footer {
  text-align: center;
  background-color: #2c3e50;
  color: #fff;
  padding: 10px 0;
}

/* Styles pour la ligne de navigation */
.navigation {
  background-color: #2c3e50;
  color: #fff;
  padding: 10px 0;
}

.navigation ul {
  list-style-type: none;
  text-align: center;
  padding: 0;
}

.navigation li {
  display: inline;
  margin-right: 20px;
}

.navigation a {
  text-decoration: none;
  color: #fff;
  font-weight: bold;
  font-size: 16px;
  transition: color 0.3s;
}

.navigation a:hover {
  color: #f39c12; /* Couleur de survol */
}

/* Ajoutez ces styles CSS à votre feuille de style existante */

.categories-and-brands {
  display: flex; /* Utilisez un affichage flex pour créer deux colonnes */
  justify-content: space-between; /* Espacez les éléments de manière égale entre les colonnes */
  margin-top: 20px; /* Marge supérieure pour espacer de la section "À Propos de Nous" */
}

.categories,
.brands {
  flex: 1; /* Chaque colonne prend 50% de l'espace disponible */
  padding: 10px;
  border: 1px solid #ccc;
  text-align: center; /* Centrer le contenu horizontalement */
  display: flex;
  flex-direction: column; /* Aligner les éléments en colonnes */
  flex-wrap: wrap; /* Permettre l'enroulement des éléments sur plusieurs lignes */
}

.categories h2,
.brands h2 {
  font-size: 18px;
  margin-bottom: 10px;
}

.categories ul,
.brands ul {
  list-style: none;
  padding: 0;
  display: flex;
  flex-wrap: wrap; /* Permettre l'enroulement des éléments sur plusieurs lignes */
}

.categories li,
.brands li {
  margin-bottom: 5px;
  display: inline-block; /* Afficher les éléments en ligne */
  background-color: #00FF00; /* Fond vert */
  padding: 5px; /* Ajoutez de l'espace autour du texte */
  margin-right: 10px; /* Espacement entre les éléments */
  flex-basis: calc(20% - 10px); /* 20% de largeur avec 10px d'espace entre les éléments */
  border-radius: 10px; /* Coins arrondis */
}

.product-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 20px;
}

.product {
  flex-basis: calc(25% - 20px); /* 25% de largeur avec 20px d'espace entre les produits */
  background-color: #fff;
  border: 1px solid #ddd;
  padding: 20px;
  text-align: center;
  box-sizing: border-box; /* Inclut les marges dans la largeur pour éviter les débordements */
  margin-bottom: 20px; /* Espacement entre les produits */
}










