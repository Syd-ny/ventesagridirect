{% extends 'frontoffice/base.html.twig' %}

{% block title %}Accueil - md-distribution{% endblock %}

{% block content %}

    <section class="products">
        <h2>En Vedette</h2>
        {% for product in products %}
            {% if product.ordre is not null %}
                <div class="product">
                    {% if product.picture %}
                        <img src="{{ asset('uploads/images/' ~ product.picture) }}" alt="{{product.name}}">
                    {% endif %}
                    <h3>{{ product.name }}</h3>
                    <p>{{ product.description }}</p>
                </div>
            {% endif %}
        {% endfor %}
    </section>
    
    <section class="about-us">
        <h2>À Propos de Nous</h2>
        <p>Nous sommes dédiés à fournir les meilleurs produits pour le bien-être de vos animaux. Notre équipe d'experts s'engage à vous offrir des produits de qualité supérieure.</p>
    </section>
    
    <section class="categories-and-brands">
        <div class="categories">
            <h2>Catégories</h2>
            <ul>
                {% for category in categorie %}
                    <li><a href="{{ path('products_by_category', {'categoryId': category.id}) }}">{{ category.name }}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="brands">
            <h2>Marques</h2>
            <ul>
                {% for marque in marque %}
                    <li><a href="{{ path('products_by_marque', {'marqueId': marque.id}) }}">{{ marque.nom }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </section>
{% endblock %}
