{% extends 'backoffice/main/base.html.twig' %}

{% block title %}{{ categorie.name }}{% endblock %}

{% block body %}
    <h1 class="mt-3 mx-3">{{ categorie.name }}</h1>

    {#% include "backoffice/fragments/_flash_message.html.twig" %#}

    <table class="table table-hover">
        <tbody>
            <tr class="table-active">
                <th scope="col">Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr class="table-active">
                <th scope="col">Nom</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_list') }}" class="btn btn-sm btn-outline-info mx-3">Retour à la liste</a>

    {# Edition of character not yet optimized #}
    {# <a href="{{ path('app_backoffice_categorie_editcategories', {'id': categorie.id}) }}" class="btn btn-sm btn-outline-warning mx-3">Modifier</a> #}
{% endblock %}